<h1> Students</h1>
<%= link_to "Add Student", new_student_path %>
<br /><br />

<% @students.each do |student| %>
    <div class="student">
     <%= link_to student.name, student %>
     </div>
<% end %>